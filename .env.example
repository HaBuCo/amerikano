# Set before producing a release build. No secrets belong in EXPO_PUBLIC values.
# Development auto-detects the Expo host and uses ws://HOST:8090.
EXPO_PUBLIC_ROOM_SERVER=wss://your-room-server.example.com
